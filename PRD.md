# 학생 평가문장 생성기 제품 요구사항

> 상태: 구현 기준선 초안  
> 아이디어 원본: `idea.txt`  
> 작업명: 학생 평가문장 생성기

## 1. 문서 목적

이 문서는 초등학교 교사가 학생 식별정보를 입력하지 않고 교과 평가 문장, 행동특성 및 종합의견, 창의적 체험활동 자율활동 문장을 생성·검수·확정·내보낼 수 있는 웹앱의 제품 요구사항 기준선이다. 구현 순서와 기술 결정은 `plan.md`, 화면과 상호작용은 `product-design.md`, 시각 정체성은 `design.md`를 따른다.

## 2. 제품 목표

- 평가계획서와 교사의 최소 입력만으로 나이스에 옮겨 적을 수 있는 한국어 평가 문장을 빠르게 만든다.
- 학생 이름·학번·반·연락처를 수집하지 않고 학생을 순번으로만 다룬다.
- 모델 출력에 의존하지 않고 서버 규칙 검증과 의미 검수로 길이·형식·중복·근거 부합을 확인한다.
- 생성 결과를 브라우저에 복구 가능하게 저장하되 서버 데이터베이스에는 영구 저장하지 않는다.
- Gemini 키를 브라우저에 노출하지 않고, 서로 다른 세 Google 프로젝트의 키로 제한적인 장애조치를 제공한다.

## 3. 대상 사용자와 핵심 문제

### 3.1 주요 사용자

- 한 학급의 평가 문장을 작성하는 초등학교 교사
- 학생 명단이나 관찰 원문을 외부 서비스에 저장하지 않으면서 반복 문장 작성 시간을 줄이고 싶은 사용자

### 3.2 해결할 문제

- 평가요소마다 다수 학생의 문장을 서로 다르게 작성하는 반복 작업이 크다.
- 행동특성 문장을 만들 때 핵심 특징을 자연스러운 네 문장으로 구성하기 어렵다.
- 자율활동 주제를 학생별로 배정하고 문장을 만드는 과정이 번거롭다.
- 생성형 모델의 길이 위반, 영어·마크다운 혼입, 근거 없는 사실, 학생 간 중복을 교사가 직접 찾아야 한다.
- 클라이언트 직접 API 호출은 API 키와 외부 호출 구조를 노출하고 비용 남용 위험을 만든다.

## 4. 성공 기준

- 세 핵심 기능의 대표 시나리오가 업로드·입력부터 검수·확정·XLSX 출력까지 중단 없이 완료된다.
- 자동 확정되는 모든 문장이 문장 수, 목표 글자 수 ±5자, 한국어 전용, 마침표, 명사형 종결, 금지 표식 규칙을 통과한다.
- 완전 동일 문장 또는 유사도 85점 이상 문장은 자동 확정되지 않는다.
- 부분 실패가 발생해도 성공한 결과는 유지되고 실패한 묶음만 재시도할 수 있다.
- 프로덕션 클라이언트 번들·소스맵·브라우저 요청에 Gemini 키, 키 슬롯, 모델명, Google API 주소와 시스템 프롬프트가 포함되지 않는다.
- 저장된 작업은 같은 브라우저에서 복구·초기화·전체 삭제할 수 있고, 서버 데이터베이스에는 생성 결과가 남지 않는다.
- 세 키 장애조치가 정의된 오류에서만 1→2→3 순으로 동작하고 성공 또는 마지막 키에서 중단된다.

## 5. 용어

| 용어 | 정의 |
| --- | --- |
| 학생 순번 | 개인정보가 아닌 `학생 1`, `학생 2` 형태의 작업용 인덱스 |
| 최종 문단 | 한 학생 순번에 대해 기능별 문장을 연결한 결과 |
| 목표 글자 수 | 공백·줄바꿈·문장부호를 제외한 한 문장당 목표 길이 |
| 확정 | 교사가 생성·검수 결과를 확인하고 사용할 결과로 승인한 상태 |
| 키 슬롯 | 서버 환경 변수의 `GEMINI_API_KEY_1`부터 `_3`까지의 순서 |
| 작업 묶음 | Vercel 함수 제한을 고려해 한 배치를 교과 또는 일정 학생 수로 나눈 요청 단위 |

## 6. 기능 요구사항과 인수 조건

### 6.1 공통 화면과 데이터

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-001 | 웹앱과 내비게이션 | 최신 데스크톱 브라우저와 반응형 화면에서 실행된다. 주요 내비게이션에는 기능 1·2·3만 있고 별도 설정 탭이 없다. |
| FR-002 | 비식별 학생 순번 | 이름·학번·반·연락처 입력 및 학생 명단 업로드 UI가 없다. 입력한 학생 수만큼 `학생 1`부터 연속 순번이 생성된다. |
| FR-003 | 접근 제어 | 인증되지 않은 사용자는 생성·문서 분석 API를 호출할 수 없다. MVP는 원격 사용자 DB 없이 서버 환경변수 기반 공유 접근 코드와 서명된 HttpOnly 쿠키를 기본안으로 하며, Vercel 접근 제한으로 대체할 수 있다. |
| FR-004 | 공통 작성 옵션 | 명사형 종결, 금지·권장 표현과 학교별 작성 원칙의 기본 템플릿이 모든 기능에 적용된다. 각 기능의 접이식 공통 작성 옵션에서 보충·기본값 복원이 가능하고 변경값은 공통 저장된다. |
| FR-005 | 브라우저 자동 저장 | 기능별 입력, 학생 수, 선택값, 주제 배정, 생성 문장, 검수·확정 상태, 템플릿, 수정 시각과 스키마 버전이 `localStorage`에 JSON으로 저장되고 재방문 시 복원된다. 복원 완료 전 기본 상태가 저장 데이터를 덮어쓰지 않는다. |
| FR-006 | 저장 범위와 한계 | API 키, 학생 식별정보, 업로드 파일·Base64, 시스템 프롬프트, Gemini 원문 응답은 저장하지 않는다. 공용 PC 평문 저장·사이트 데이터 삭제·용량 제한을 안내하고 저장 실패 시 XLSX 내보내기 또는 이력 정리를 제안한다. |
| FR-007 | 데이터 관리 | 기능 화면의 초기화는 해당 기능만 지우며 사전 확인한다. 헤더의 브라우저 데이터 관리 메뉴에서 현재 작업·전체 저장 데이터·내보내기를 제공하고 전체 삭제는 공통 옵션과 이력까지의 영향을 재확인한다. |
| FR-008 | 이력과 마이그레이션 | 현재 작업과 최근 버전만 앱이 정한 상한 내에서 보관한다. 오래된 미확정 이력을 지우기 전에 알리고, 스키마 버전으로 마이그레이션하거나 호환되지 않는 데이터만 안전하게 초기화한다. |
| FR-009 | 서비스 설정 오류 | 필수 서버 환경변수 또는 사용 가능한 키가 없으면 생성 기능을 비활성화하고 일반화된 오류와 진단 코드만 보여준다. API 키 입력을 요구하는 우회 흐름은 없다. |

### 6.2 기능 1 — 평가계획서 기반 교과 평가

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-101 | 평가계획서 업로드 | PDF, 이미지, DOCX, PPTX, XLSX를 한 개 또는 여러 개 업로드할 수 있다. HWP/HWPX는 거부하고 PDF 변환 안내를 표시한다. |
| FR-102 | 파일 검증과 서버 처리 | 서버가 실제 파일 형식·MIME·용량·손상·암호화 여부를 확인한다. PDF·이미지는 Gemini 문서 이해로, DOCX·PPTX·XLSX는 서버에서 텍스트·표 구조를 추출한 뒤 Gemini로 처리한다. 원본은 영구 저장하지 않는다. |
| FR-103 | 평가 구조 추출 | 교과명, 학년, 학기, 평가 영역, 성취기준, 평가요소와 문서에 있는 경우 ‘상’에 해당하는 최고 단계 평가 수준이 구조화된 스키마로 추출되고 교사가 생성 전 검토·수정할 수 있다. 중·하 수준은 생성 입력으로 사용하지 않는다. |
| FR-104 | 중복 병합과 충돌 복구 | 문자열 및 의미 비교로 중복 항목을 자동 병합한다. 불확실한 충돌 항목만 생성에서 제외하고 오류 이유를 표시하며, 수정 후 해당 항목만 재처리할 수 있다. 정상 항목은 계속 처리한다. |
| FR-105 | 학생 수와 ‘상’ 기준 자동 적용 | 양의 정수 학생 수로 순번을 만든다. 모든 교과·순번·평가요소에는 서버가 ‘상’ 성취 기준을 고정 적용한다. 상·중·하 선택 UI, 수준 매트릭스, 일괄·개별 수준 변경과 클라이언트 수준 입력 필드는 없다. 점수·점수 구간·교사 메모도 받지 않는다. |
| FR-106 | 교과 배치 생성 | 확정된 모든 교과를 한 배치로 시작할 수 있다. 각 평가요소당 한 문장, 교과별 한 문단을 학생 수만큼 만들고 모든 문장은 명사형으로 끝난다. |
| FR-107 | ‘상’ 중심 근거 제한 | 문서의 ‘상’ 평가 수준이 있으면 성취기준·평가요소와 함께 사용하고, 없으면 두 근거 안에서 높은 성취가 드러나게 작성한다. 입력에 없는 활동·수치·수상·역할·성취 사실은 추정하지 않으며, 모든 순번에 같은 ‘상’ 기준을 적용해도 문장 구조와 강조점은 실질적으로 달라야 한다. |
| FR-108 | 교과별 결과 단위 | 조회·확정·선택 재생성·복사·개별 XLSX는 선택한 교과 결과만 대상으로 하며 다른 교과 결과를 섞지 않는다. |

### 6.3 기능 2 — 핵심 키워드 기반 행동특성

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-201 | 키워드 입력 방식 | 관찰 기록, 날짜, 관찰 XLSX 업로드와 자동 분류 기능은 없다. 학교생활·교우 관계·학업 태도·장점 및 발전 가능성별 기본 키워드를 코드 데이터 모듈에서 제공한다. |
| FR-202 | 학생별 복수 선택 | 학생 수만큼 카드를 만들고 기본 키워드 복수 선택과 사용자 정의 키워드 칩 추가·삭제를 지원한다. 학생 간 선택 복사가 가능하며 학생별 최소 한 개의 키워드가 있어야 생성할 수 있다. |
| FR-203 | 네 문장 생성 | 학생별로 학교생활 전반, 교우 관계, 학업 태도, 장점 또는 발전 가능성이 드러나는 네 문장을 생성한다. 키워드를 기계적으로 나열하지 않고 각 문장에 반영된 키워드를 검토 화면에서 연결해 보여준다. |
| FR-204 | 사실 생성 제한 | 키워드에 없는 사건·역할·수치·성취·변화·행동 사례를 만들지 않는다. 부정적 사용자 키워드는 낙인 없이 중립적으로 표현하고 확인되지 않은 노력이나 개선을 추가하지 않는다. |

### 6.4 기능 3 — 창의적 체험활동 자율활동

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-301 | 활동 주제 입력 | 사용자가 주제를 직접 입력하거나 지원 문서를 서버로 보내 Gemini가 주제를 추출할 수 있다. 중복을 정리한 유효 주제가 네 개 미만이면 생성 단계로 갈 수 없다. |
| FR-302 | 학생별 무작위 배정 | 학생 수만큼 각기 다른 주제 네 개를 배정한다. 한 학생 안에서는 중복하지 않고 학생 사이 중복은 허용한다. 배정 결과를 생성 전에 표시한다. |
| FR-303 | 배정 검토와 승인 | 사용자가 학생별 주제를 교체하거나 전체·개별 재배정할 수 있고 명시적으로 승인하기 전에는 문장을 생성하지 않는다. |
| FR-304 | 주제별 문장 생성 | 승인한 네 주제마다 한 문장씩 네 문장의 문단을 만든다. 일반적이고 긍정적인 참여 수준을 사용하며 승인되지 않은 활동이나 구체적 사건을 추가하지 않는다. |

### 6.5 공통 생성·검수·확정

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-401 | 목표 길이 | 기능 1·2·3 모두 한 문장당 목표 글자 수를 받는다. 공백·줄바꿈·문장부호를 제외해 계산하고 허용 범위는 목표 ±5자로 고정한다. 범위 밖 문장만 자동 재생성한다. |
| FR-402 | 출력 형식 | 최종 결과에는 본문만 포함하고 번호·불릿·마크다운·제목·설명·불필요한 기호가 없다. 모든 문장은 마침표로 끝나고 명사형 종결 규칙을 지킨다. |
| FR-403 | 한국어와 숫자 | 영어 교과를 포함해 영어 알파벳·외국어 표현을 넣지 않는다. 숫자는 공식명·고유명사·필수 의미를 제외하고 한글 표현을 우선한다. |
| FR-404 | 구조화 응답과 입력 격리 | 모델 응답은 서버 스키마를 통과해야 한다. 사용자 옵션·키워드·문서 텍스트는 길이·문자·개수를 제한한 데이터로만 전달되며 시스템 지시·모델·보안·스키마를 덮어쓸 수 없다. |
| FR-405 | 규칙 및 의미 검수 | 서버 규칙은 문장 수·길이·종결·금지 표현·영어·표식·마침표를 검사한다. Gemini 검수는 근거 부합, 근거 없는 사실, 과장·단정·낙인·차별을 검사한다. 실패 이유와 수정 제안을 보여주고 문제 문장만 재생성한다. |
| FR-406 | 중복 검수 | 완전 동일·표현/구조 반복·의미 중복을 한 학생 내부와 전체 학생 사이에서 비교한다. 점수와 겹친 부분을 보여주며 85점 이상은 해당 문장만 최대 세 번 재생성한다. 이후에도 중복이면 교사 판단 전 자동 확정하지 않는다. |
| FR-407 | 근거 표시 | 기능 1은 성취기준·평가요소·자동 적용된 ‘상’ 기준, 기능 2는 기본·사용자 키워드, 기능 3은 승인 활동 주제를 결과와 연결해 보여준다. 기능 1의 ‘상’ 기준은 사용자 선택값처럼 표시하지 않으며 기능 2에는 관찰 날짜·관찰 기록·추론 표시가 없다. |
| FR-408 | 교사 편집과 확정 | 특정 단어·문장을 직접 수정하거나 선택 문장만 재생성할 수 있다. 직접 수정 결과도 규칙 검사를 다시 거치며 교사가 확정해야 완성 상태가 된다. |

### 6.6 배치·복구·내보내기

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-501 | 배치 진행과 부분 실패 | 생성·형식 검사·의미 중복·수정 단계를 구분해 진행 상태를 보여준다. 일부 교과·학생·문장이 실패해도 나머지는 계속되며 실패 항목만 재시도할 수 있다. |
| FR-502 | 요청 분할과 중복 방지 | 교과 또는 일정 학생 수 단위로 요청을 나눈다. 전송 중 생성 버튼을 잠그고 작업 ID와 완료 상태를 기록한다. 응답 전 연결이 끊긴 요청은 정확히 한 번 실행을 보장하지 않으며 추가 결과·비용 가능성을 안내한다. |
| FR-503 | 통합·개별 XLSX | 확정 결과를 통합 또는 선택 교과/기능 XLSX로 만든다. 통합 파일은 기능 1 교과별 시트와 기능 2·3 시트를 사용하며 학생 순번, 문단, 확정 상태와 기능별 근거를 포함한다. 이름·학번은 없다. |
| FR-504 | 명시적 클립보드 복사 | 사용자 클릭 시 개별 문단 또는 현재 교과·기능 전체를 순번대로 줄바꿈/표 형식으로 복사한다. 권한 실패 시 선택 가능한 텍스트를 제공한다. 나이스 자동 붙여넣기·전역 단축키·자동 탭 이동은 없다. |

### 6.7 Gemini 서버 연동과 키 장애조치

| ID | 요구사항 | 인수 조건 |
| --- | --- | --- |
| FR-601 | 서버 전용 모델 호출 | 모든 생성·재생성·검수·문서 분석은 같은 출처 POST API와 Vercel Functions에서 수행한다. 브라우저 요청에는 키·모델명·키 슬롯·외부 URL이 없고, Google SDK와 프롬프트는 클라이언트 번들에 없다. |
| FR-602 | 세 프로젝트 키 풀 | Production은 서로 다른 Google 계정·프로젝트의 키를 `_1`, `_2`, `_3`에 연결한다. 키 1과 모델은 필수, 키 2·3은 운영상 구성하되 앱은 누락 시 정상 키로 축소 운영한다. 빈 값과 중복 키는 제외한다. |
| FR-603 | 순차 장애조치 | 인증·권한 오류 또는 429에서 키 1→2→3 순으로 전환하고 성공 즉시 중단한다. 짧은 429는 실행 시간 내 한 차례 백오프할 수 있다. 마지막 키 실패 후 순환하지 않는다. |
| FR-604 | 비전환 오류 | 400·404·콘텐츠 거부는 즉시 반환한다. 5xx/503은 제한된 백오프만 하고 키를 순환하지 않는다. 결과 생성 여부가 불명확한 타임아웃·연결 종료는 다른 키로 즉시 재호출하지 않는다. |
| FR-605 | 요청 범위 상태 | 공유 키 상태 저장소가 없으므로 장애조치 상태는 현재 요청에만 적용한다. 새 서버리스 인스턴스가 다시 키 1부터 시작할 수 있음을 운영 한계로 기록한다. |
| FR-606 | 비밀·캐시·로그 | 키 값·해시·접두사·계정/프로젝트 식별정보를 응답·로그·localStorage에 남기지 않는다. 슬롯 번호·정규화 오류·전환 여부·소요 시간만 관측하고 API 응답은 `Cache-Control: no-store`를 사용한다. |
| FR-607 | 비용과 한도 | 생성 전 예상 문장 수를 보여주고 확인받는다. 재생성·백오프·키 순환에 상한을 두며 프로젝트별 및 합산 사용량·비용·성공률·429를 모니터링한다. |

## 7. 비기능 요구사항

| ID | 요구사항 |
| --- | --- |
| NFR-001 | 개인정보: 학생 식별정보를 입력받지 않고 요청 원문·생성 결과를 서버 DB와 애플리케이션 로그에 영구 저장하지 않는다. 실제 Vercel·Google 보존 정책은 공개 전 개인정보 안내에 반영한다. |
| NFR-002 | 보안: 서버 입력 재검증, MIME/크기 제한, Origin 검사, CSRF 방어, 접근 제어, 속도·사용량 제한, CSP, 보안 헤더와 일반화 오류를 적용한다. 원시 HTML 렌더링을 금지한다. |
| NFR-003 | 접근성: WCAG 2.1 AA 수준을 목표로 키보드 조작, 명확한 포커스, 상태 텍스트, 최소 44px 조작 영역, 200% 확대와 모션 감소를 지원한다. |
| NFR-004 | 반응형: 데스크톱을 주 사용 환경으로 최적화하되 태블릿·모바일에서 핵심 입력·검토·내보내기가 차단되지 않는다. |
| NFR-005 | 신뢰성: 부분 성공 보존, 제한된 재시도, 오류 분류, 복원 가능한 로컬 작업 상태와 사용자가 통제하는 삭제를 제공한다. |
| NFR-006 | 성능: 대형 표·결과 목록은 필요한 범위만 렌더링하고 XLSX 라이브러리는 지연 로드한다. localStorage 쓰기는 디바운스하며 긴 모델 작업은 묶음 단위로 진행한다. |
| NFR-007 | 유지보수: 프롬프트, 규칙 검사, 키워드 데이터, 문서 파서, Gemini 장애조치, 저장 스키마와 UI를 분리하고 공유 Zod 스키마와 TypeScript strict를 사용한다. |
| NFR-008 | 디자인: `design.md`의 D.Y. Kim 시스템과 승인 자산을 변경 없이 사용하고 제품명이 제작자 서명보다 우선한다. |
| NFR-009 | 배포: 소스는 기존 GitHub `evaluator` 저장소에서 관리하고 Vercel 프로젝트의 Git 배포 소스로 연결한다. GitHub 작업은 GitHub CLI, Vercel 연결·환경변수·배포 확인은 Vercel CLI로 수행한다. 기능 브랜치와 Pull Request는 Preview를, 검증된 변경의 `main` 병합은 Production을 자동 배포한다. 직접 `vercel deploy --prod`는 정상 릴리스 경로로 사용하지 않으며 비밀값은 Vercel 환경변수로만 주입한다. |
| NFR-010 | 검증: 단위·컴포넌트·API 통합·Playwright E2E·빌드·클라이언트 비밀 스캔이 자동화되어야 한다. |

## 8. 주요 사용자 흐름

### 8.1 최초 진입과 복원

1. 사용자가 접근 제어를 통과한다.
2. 앱이 브라우저 저장 가능성과 서버 생성 가능 상태를 확인한다.
3. 저장된 작업이 있으면 요약과 수정 시각을 보여주고 복원하거나 버릴 수 있게 한다.
4. 기능 1·2·3 중 하나를 선택한다.

### 8.2 기능 1

1. 평가계획서 파일을 선택하고 업로드 전 형식·개수를 확인한다.
2. 서버 추출 결과와 충돌 목록을 검토·수정한다.
3. 학생 수와 목표 길이를 설정하고 모든 결과에 ‘상’ 기준이 자동 적용됨을 확인한다. 수준을 선택하거나 변경하는 단계는 없다.
4. 예상 문장 수를 확인하고 배치 생성을 시작한다.
5. 진행·부분 실패·검수·중복 결과를 확인하고 필요한 문장만 수정·재생성한다.
6. 교과별로 확정한 뒤 복사 또는 XLSX를 내려받는다.

### 8.3 기능 2

1. 학생 수와 목표 길이를 입력한다.
2. 학생 순번별 기본 키워드를 복수 선택하고 필요한 사용자 키워드를 추가한다.
3. 선택 내용을 다른 순번에 복사하거나 개별 조정한다.
4. 네 문장을 생성하고 키워드 반영 근거·검수·중복 결과를 확인한다.
5. 수정·재생성·확정 후 복사 또는 XLSX를 내려받는다.

### 8.4 기능 3

1. 활동 주제를 직접 입력하거나 문서에서 추출한다.
2. 유효 주제를 네 개 이상 확보하고 학생 수·목표 길이를 입력한다.
3. 학생별 네 주제를 무작위 배정한 뒤 교체·재배정·승인한다.
4. 승인 결과로 네 문장을 생성하고 검수·수정·확정한다.
5. 복사 또는 XLSX를 내려받는다.

## 9. MVP 범위

- 기능 1·2·3의 전체 입력, 생성, 검수, 확정, 복사, XLSX 흐름
- PDF·이미지·DOCX·PPTX·XLSX 입력과 HWP/HWPX 안내
- localStorage 자동 저장·복원·삭제·제한된 이력
- 서버 전용 Gemini 3.5 Flash Lite와 최대 세 키 장애조치
- 규칙 검수, 의미 검수, 85점 중복 임계치, 선택 재생성
- 배치 분할, 부분 실패와 실패 항목 재시도
- D.Y. Kim 디자인 시스템, 접근성, 반응형 UI
- 최소 접근 제어와 비용·사용량 보호

## 10. 명시적 제외 범위

- 학생 이름·학번·반·연락처 저장 및 학생 명단/XLSX 업로드
- 관찰 기록·날짜 누적, 관찰 XLSX 업로드, 관찰 자동 분류
- HWP/HWPX 직접 파싱
- 나이스 자동 붙여넣기, 전역 단축키, 키보드·마우스 자동화
- 사용자 API 키 입력, 모델 선택, 외부 API 주소 설정과 별도 설정 탭
- 서버 데이터베이스, 계정별 클라우드 동기화, 기기 간 작업 공유
- 장기 서버 작업 이력, 전역 키 냉각 상태와 정확히 한 번 실행 보장
- 학생별 점수·점수 구간·교사 메모
- Gemini 외 다른 생성 모델 폴백
- 모바일 네이티브 앱과 Windows 전용 설치 프로그램

## 11. 가정

- 제품명은 확정 전까지 ‘학생 평가문장 생성기’를 사용한다.
- 서비스 운영자는 서로 다른 세 Google 계정·프로젝트와 키를 정당하게 관리한다.
- 교사는 학생 순번을 실제 명단과 외부에서 대조하며 앱에는 식별정보를 입력하지 않는다.
- 서버에 결과를 영구 저장하지 않는 대신 브라우저 데이터 삭제·용량 초과 위험을 사용자가 이해한다.
- MVP 접근 제어는 원격 사용자 DB가 필요 없는 공유 접근 코드 방식을 우선 설계한다.
- 문서·학생 수·목표 글자 수의 하드 제한은 구현 전 Vercel과 Gemini 한도 측정 후 상수로 확정한다.

## 12. 열린 질문

| ID | 질문 | 기본 처리 |
| --- | --- | --- |
| OQ-001 | 최종 제품명은 무엇인가? | 작업명 유지 |
| OQ-002 | 접근 제어를 공유 접근 코드로 할지 Vercel 보호로 할지? | 공유 접근 코드 + HttpOnly 세션을 계획 기준으로 사용 |
| OQ-003 | 학생 수, 파일 개수·크기, 목표 글자 수의 최소·최대값은? | 구현 스파이크와 비용 측정 후 서버 상수로 확정 |
| OQ-004 | localStorage 최근 이력 보관 개수는? | 저장량 측정 후 안전한 고정 상한 적용, 사용자 설정은 제공하지 않음 |
| OQ-005 | 통합 XLSX의 정확한 열 순서와 파일명 규칙은? | `product-design.md`의 초안을 사용자 예시 파일로 검증 |
| OQ-006 | 공식 학교 문장 템플릿과 금지·권장 표현의 초기 목록은? | 코드 데이터로 분리하고 출시 전 교사 검수 |

## 13. 출시 전 제품 인수 기준

- 모든 `FR-*` 요구사항에 자동 테스트 또는 수동 검증 기록이 연결되어 있다.
- `NFR-001`부터 `NFR-010`까지의 증거가 릴리스 체크리스트에 남아 있다.
- 실제 학생정보가 없는 고정 fixture로 기능별 정상·부분 실패·복원·내보내기 E2E가 통과한다.
- 세 키의 실제 값 없이 모의 오류로 장애조치 순서와 비전환 오류를 검증하고, 배포 후에는 최소 비식별 요청으로 각 슬롯만 점검한다.
- `design.md`와 `assets/signature/final/manifest.json` 해시가 유지되고 승인 favicon·Creator credit이 적용된다.
- 기능 브랜치 또는 Pull Request가 만든 Vercel Preview에서 홈·접근 제어·세 기능·API 오류 경로를 확인하고, 검증된 Pull Request만 `main`에 병합한다. 해당 병합으로 생성된 Production 배포를 Vercel CLI로 확인한다.
