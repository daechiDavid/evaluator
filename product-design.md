# 학생 평가문장 생성기 제품 디자인

`design.md`는 이 프로젝트의 유일한 canonical D.Y. Kim 시각 정체성 기준이며, `assets/signature/final/`의 번들 고정 자산은 비율·색·형태를 바꾸거나 다시 그리지 않고 그대로 사용한다.

## 1. 문서 역할

이 문서는 `PRD.md`의 요구사항을 화면 구조, 사용자 흐름과 컴포넌트로 변환한다. 색상·타이포그래피·간격·모션·접근성과 브랜드 자산의 최종 기준은 반복해서 정의하지 않고 `design.md`에 위임한다.

## 2. 디자인 목표

- 교사가 현재 단계와 다음 행동을 한눈에 이해한다.
- 대량 입력과 검수는 촘촘하지만 답답하지 않은 정보 구조로 제공한다.
- 생성형 AI보다 교사의 근거·검토·확정이 시각적으로 우선한다.
- 개인정보를 입력하지 않는 제품 원칙과 브라우저 저장 한계를 필요한 순간에 설명한다.
- 오류가 전체 작업을 지우지 않으며 실패 항목으로 바로 이동할 수 있다.

## 3. 정보 구조

### 3.1 전역 구조

```text
접근 화면
└─ 앱 셸
   ├─ 헤더
   │  ├─ 제품명
   │  ├─ 기능 1
   │  ├─ 기능 2
   │  ├─ 기능 3
   │  └─ 브라우저 데이터 관리
   ├─ 현재 기능 작업 영역
   │  ├─ 기능 소개와 개인정보 안내
   │  ├─ 공통 작성 옵션
   │  ├─ 기능별 단계
   │  └─ 결과·검수·확정
   └─ 푸터
      └─ 승인된 D.Y. Kim Creator credit
```

- 별도 설정 탭, API 키 입력, 모델 선택과 외부 API 주소 화면은 만들지 않는다.
- 기능 전환 시 저장되지 않은 작업은 즉시 localStorage에 반영한 뒤 이동한다.
- 헤더의 데이터 관리 메뉴는 설정 화면이 아니라 현재 브라우저 데이터의 삭제·내보내기 작업만 담당한다.

### 3.2 라우트 초안

| 라우트 | 역할 |
| --- | --- |
| `/access` | 공유 접근 코드 방식이 채택될 경우의 접근 화면 |
| `/feature-1` | 평가계획서 기반 교과 평가 |
| `/feature-2` | 핵심 키워드 기반 행동특성 |
| `/feature-3` | 자율활동 주제 기반 문장 |

루트 `/`는 인증 상태와 최근 작업에 따라 마지막 사용 기능 또는 기능 1로 이동한다. API 라우트는 화면 정보 구조에 노출하지 않는다.

## 4. 앱 셸

### 4.1 헤더

- 제품명은 접근 가능한 실제 텍스트로 표시하고 제작자 서명보다 시각적으로 크게 둔다.
- 현재 기능은 라벨, 굵기와 기능 상태 Point를 함께 사용해 색만으로 구분하지 않는다.
- 데스크톱에서는 세 기능을 가로 탭으로, 작은 화면에서는 동일 항목의 메뉴 버튼으로 제공한다.
- 우측 ‘브라우저 데이터 관리’ 버튼은 저장 상태를 설명하는 텍스트와 함께 제공한다.
- 기능 탭과 데이터 관리 외의 설정 아이콘은 두지 않는다.

### 4.2 푸터와 고정 자산

- 밝은 화면은 `assets/signature/final/dyk-credit-on-light.svg`, 어두운 화면은 `dyk-credit-on-dark.svg`를 사용한다.
- 파비콘은 `dyk-favicon.svg`와 승인 PNG 파생 파일을 그대로 사용한다.
- 제품 로고를 새로 만들지 않고, 작은 D. 심볼이 필요한 경우 승인된 `dyk-symbol-*`만 사용한다.
- 자산에 CSS 필터, 그림자, 마스크, 재색상과 애니메이션을 적용하지 않는다.

### 4.3 전역 상태 표시

- 상단 저장 상태는 ‘저장 중’, ‘이 브라우저에 저장됨’, ‘저장 실패’로 표시한다.
- 서버 생성 가능 상태는 정상일 때 숨기고, 환경 설정 오류 또는 접근 불가일 때만 배너로 표시한다.
- API 키 개수·슬롯·프로젝트·Google 오류 원문은 사용자 화면에 표시하지 않는다.
- 개인정보 안내는 학생 이름·학번을 입력하지 말라는 짧은 문구로 각 기능 첫 입력 영역에 반복한다.

## 5. 공통 상호작용 패턴

### 5.1 단계 레이아웃

각 기능은 다음 네 단계 패턴을 공유한다.

1. 입력 또는 자료 준비
2. 구조·배정 검토
3. 생성 진행
4. 결과 검수·확정·내보내기

완료한 단계는 접을 수 있지만 오류가 있는 단계는 요약에 오류 수와 이동 링크를 남긴다. 현재 단계의 주요 행동은 한 개만 가장 강하게 보이게 한다.

### 5.2 공통 작성 옵션

- 기능 소개 아래 접힌 패널로 제공한다.
- 현재 적용 중인 사용자 보충 규칙 수를 요약에 표시한다.
- 금지 표현, 권장 표현과 추가 작성 원칙을 편집할 수 있다.
- ‘기본값 복원’은 영향을 설명하고 확인받는다.
- 변경 내용이 다른 기능에도 적용된다는 설명을 항상 보인다.

### 5.3 학생 순번

- 학생 이름 입력란은 없다.
- 데스크톱 표·카드의 첫 열/제목에 `학생 1`, `학생 2`를 고정 표시한다.
- 결과 복사와 XLSX 순서는 이 순번과 일치한다.
- 학생 수를 줄일 때 삭제될 순번의 입력·결과 수를 보여주고 확인받는다.

### 5.4 생성 전 확인

- 학생 수, 교과/주제/키워드 수, 평가요소 수와 예상 문장 수를 요약한다.
- 생성 중 발생할 수 있는 추가 재생성과 비용을 설명한다.
- 생성 버튼은 필수 입력과 승인 조건을 만족해야 활성화된다.

### 5.5 결과 카드

각 결과 카드는 다음 순서를 사용한다.

1. 학생 순번과 확정 상태
2. 최종 문단 편집 영역
3. 문장별 글자 수와 규칙 상태
4. 반영 근거
5. 중복·의미 검수 정보
6. 문장 재생성, 복사, 확정 행동

정상 상태는 과도하게 강조하지 않고 오류·검토 필요 상태만 기능색과 텍스트로 분명히 표시한다.

## 6. 접근 화면

`PRD.md`의 기본 접근 제어안이 채택될 때만 제공한다.

- 제품명, 짧은 목적 설명, 접근 코드 입력과 계속 버튼으로 구성한다.
- API 키와 무관한 서비스 접근 코드임을 설명한다.
- 실패 횟수나 서버 보안 세부정보를 노출하지 않고 일반적인 오류를 표시한다.
- 성공 후 서명된 HttpOnly 세션으로 마지막 사용 기능으로 이동한다.
- Vercel 접근 제한으로 대체하면 이 화면은 제거한다.

## 7. 최초 진입과 작업 복원

### 7.1 저장 데이터 없음

- 세 기능의 한 줄 설명과 ‘기능 1 시작’ 기본 행동을 보여준다.
- 학생 식별정보를 받지 않는다는 원칙과 결과가 이 브라우저에 저장된다는 안내를 제공한다.

### 7.2 저장 데이터 있음

- 마지막 기능, 수정 시각, 학생 수, 생성/확정 결과 수를 요약한다.
- ‘이어서 작업’과 ‘초안 버리기’를 제공하고 후자는 확인 대화상자를 연다.
- 복원 중에는 빈 기본 상태를 localStorage에 쓰지 않고 스켈레톤만 표시한다.

### 7.3 저장 오류

- 작업은 메모리에서 계속할 수 있게 하되 ‘브라우저 저장 실패’를 고정 배너로 표시한다.
- 확정된 결과가 있으면 즉시 XLSX로 내보내고, 확정 전 문장은 선택 가능한 텍스트로 복사할 수 있게 한다.

## 8. 기능 1 화면

### 8.1 1단계 — 평가계획서 업로드

- 드래그 앤 드롭 영역과 파일 선택 버튼을 모두 제공한다.
- 지원 형식과 HWP/HWPX의 PDF 변환 안내를 업로드 전에 표시한다.
- 파일 행에는 이름, 형식, 크기, 검증 상태와 제거 버튼을 표시한다.
- 여러 파일의 전체 업로드 진행 상태와 개별 실패 이유를 구분한다.
- 학생정보가 포함된 문서를 업로드하지 말라는 경고와 서버·Google 처리 안내를 제공한다.

### 8.2 2단계 — 추출·병합 검토

- 교과별 아코디언 안에 학년, 학기, 평가 영역, 성취기준과 평가요소를 편집 가능한 필드로 표시한다.
- 문서에서 ‘상’에 해당하는 최고 단계 평가 수준을 추출한 경우에는 ‘상 기준 참고 내용’이라는 단일 필드로 보여준다. 중·하 수준을 표시하거나 선택하게 하지 않는다.
- 자동 병합 항목은 출처 수를 표시하되 매번 승인을 요구하지 않는다.
- 충돌 목록은 정상 항목과 분리하고 교과·필드·오류 이유·수정·재처리 행동을 제공한다.
- 충돌 항목이 있어도 정상 교과의 다음 단계 이동은 허용한다.
- 추출 결과가 없으면 파일 재선택과 직접 오류 확인 경로를 제공한다.

### 8.3 3단계 — 학생 수와 생성 조건

- 상단에 학생 수와 한 문장당 목표 글자 수를 둔다.
- 모든 교과·학생 순번·평가요소에 ‘상’ 성취 기준이 자동 적용된다는 짧은 안내를 표시한다.
- 상·중·하 선택, 수준 매트릭스, 교과·행·열 일괄 수준 설정과 개별 수준 변경 UI는 만들지 않는다.
- 학생 수를 입력하면 생성 대상 순번 수와 교과별 예상 문장 수를 즉시 갱신한다.

### 8.4 4단계 — 배치 생성과 결과

- 전체 교과 요약, 예상 문장 수와 생성 확인을 보여준다.
- 교과/작업 묶음별 대기·생성·형식 검사·중복 검사·완료·실패 상태를 제공한다.
- 교과 드롭다운으로 한 교과 결과만 조회한다.
- 각 결과에 성취기준, 평가요소와 자동 적용된 ‘상’ 기준 근거를 연결한다. 이를 사용자 선택값처럼 표현하지 않는다.
- ‘현재 교과 전체 복사’, ‘현재 교과 XLSX’, ‘통합 XLSX’를 구분한다.

## 9. 기능 2 화면

### 9.1 입력 영역

- 학생 수와 목표 글자 수를 먼저 입력한다.
- 관찰 기록, 날짜, 파일 업로드 영역은 만들지 않는다.
- 학생 순번 카드를 세로 목록 또는 데스크톱 2열로 표시한다.

### 9.2 키워드 선택기

- 학교생활 전반, 교우 관계, 학업 태도, 장점 및 발전 가능성의 네 그룹을 제공한다.
- 키워드는 다중 선택 칩으로 표현하고 선택 여부를 체크 표시와 배경 변화로 함께 나타낸다.
- 검색 또는 그룹 접기를 제공해 긴 목록을 탐색한다.
- 사용자 정의 입력은 Enter 또는 추가 버튼으로 칩을 만들며 길이·중복 오류를 즉시 알린다.
- 학생 카드마다 선택 수를 보여주고 최소 한 개 미만이면 오류 상태로 표시한다.
- ‘이 선택을 다른 학생에게 복사’는 대상 순번을 선택한 뒤 덮어쓸지 합칠지 확인한다.

### 9.3 결과 검토

- 네 문장을 하나의 문단으로 보여주되 문장 단위 편집·재생성이 가능하다.
- 각 문장 아래 반영된 키워드를 작은 근거 칩으로 표시한다.
- 키워드에 없는 구체적 사건·역할·수치가 감지되면 확정 버튼을 잠그고 검수 이유를 보여준다.
- 부정적 표현은 경고가 아니라 중립적 수정 제안과 함께 제시한다.

## 10. 기능 3 화면

### 10.1 주제 준비

- 직접 입력과 문서 추출을 같은 주제 목록에 합친다.
- 주제 행에는 출처, 편집, 삭제와 중복 상태를 표시한다.
- 유효 주제 수를 `현재 n개 / 최소 4개`로 보여준다.
- 네 개 미만이면 배정 버튼을 비활성화하고 필요한 수를 안내한다.

### 10.2 무작위 배정 검토

- 학생 수와 목표 글자 수 입력 뒤 각 학생에 네 주제 카드를 표시한다.
- 한 학생 내부 중복은 UI와 데이터 규칙 양쪽에서 막는다.
- 개별 주제 교체, 학생 한 명 재배정, 전체 재배정 행동을 제공한다.
- 재배정은 아직 생성되지 않은 상태에서만 자유롭게 하고 기존 승인 취소 영향을 설명한다.
- 모든 학생의 배정을 검토한 뒤 ‘배정 승인’을 명시적으로 누르게 한다.

### 10.3 생성 결과

- 승인한 네 주제와 생성한 네 문장을 일대일로 연결한다.
- 승인 이후 주제를 수정하면 해당 학생의 승인과 결과 확정 상태를 해제한다.
- 주제에 없는 구체적 사건이 검수에서 발견되면 문제 문장만 재생성할 수 있게 한다.

## 11. 배치 진행과 오류 복구

### 11.1 진행 패널

- 전체 진행률과 단계별 개수를 함께 보여준다.
- 작업 묶음별 상태를 접을 수 있는 목록으로 제공한다.
- 현재 네트워크 요청 중에는 같은 생성 버튼을 비활성화한다.
- 사용자가 페이지를 이동해도 완료 상태를 localStorage에서 복원한다.

### 11.2 부분 실패

- 상단에 성공·검토 필요·실패 수를 요약한다.
- 실패 필터와 ‘실패 항목만 재시도’를 제공한다.
- 성공 결과는 재시도에 포함하지 않는다.
- 응답 전 연결이 끊겨 처리 여부가 불명확하면 ‘결과 불명확’ 상태와 추가 호출·비용 가능성을 표시한다.

### 11.3 Gemini 장애조치 오류

- 키 슬롯 전환은 사용자에게 숨긴다.
- 모든 사용 가능 키가 실패하면 ‘현재 생성 서비스를 사용할 수 없습니다. 잠시 후 다시 시도하세요.’와 진단 코드만 제공한다.
- 입력 오류는 사용자가 수정할 필드로 연결하고, 서비스 오류와 혼동하지 않는다.
- Google 전체 장애 가능성이 있는 5xx는 반복 버튼 연타를 유도하지 않고 재시도 가능 시점을 안내한다.

## 12. 검수와 중복 화면

- 문장별 검수 상태를 형식, 근거, 표현, 중복 네 그룹으로 나눈다.
- 유사도는 100점 점수, 비교 대상 학생 순번과 겹친 표현을 보여준다.
- 85점 이상은 ‘재생성 필요’, 세 번 후에도 남으면 ‘교사 판단 필요’로 표시한다.
- 자동 재생성 횟수를 문장별로 보여주고 상한 이후 자동 호출을 중단한다.
- 직접 수정한 문장은 저장 전에 동일 검수를 다시 실행한다.
- 확정된 문장이 이후 편집되면 확정 상태를 자동 해제한다.

## 13. XLSX와 클립보드

### 13.1 XLSX

- 통합 파일과 현재 교과/기능 파일을 구분한 버튼을 제공한다.
- 생성 전 포함 시트, 결과 수와 미확정 결과 제외 여부를 미리 보여준다.
- 기본 열 순서는 학생 순번, 최종 문단, 확정 상태, 기능별 근거다.
- 기능 1 통합 파일은 `기능1_교과명`, 기능 2와 3은 각각 별도 시트를 사용한다.
- 파일 생성 중 진행 표시와 실패 후 재시도를 제공한다.

### 13.2 클립보드

- 개별 복사는 각 결과 카드 가까이에 둔다.
- 전체 복사는 현재 선택한 교과 또는 기능만 대상으로 한다.
- 줄바꿈 형식과 표 형식을 사용자가 선택할 수 있다.
- 브라우저 권한 실패 시 읽기 전용 텍스트 영역을 자동 표시하고 전체 선택 버튼을 제공한다.
- 자동 붙여넣기, 나이스 창 조작과 전역 단축키를 암시하는 문구를 사용하지 않는다.

## 14. 브라우저 데이터 관리 메뉴

| 행동 | 설명 | 확인 |
| --- | --- | --- |
| 현재 기능 초기화 | 현재 기능의 입력·초안·결과만 삭제 | 삭제 항목 수 표시 후 확인 |
| 현재 결과 내보내기 | 확정된 비식별 결과 XLSX | 즉시 실행 가능 |
| 전체 저장 데이터 삭제 | 세 기능, 공통 작성 옵션과 이력 삭제 | 평문 저장 안내와 복구 불가 재확인 |

API 키, 모델, 서버 주소, 키 슬롯과 보관 개수 설정은 이 메뉴에 포함하지 않는다.

## 15. 상태 설계

| 상태 | 사용자 경험 |
| --- | --- |
| 빈 상태 | 무엇을 입력해야 하는지와 대표 행동 한 개를 제공 |
| 로딩 | 실제 단계와 처리 대상을 표시하고 의미 없는 브랜드 애니메이션 금지 |
| 부분 성공 | 성공 결과 유지, 실패 수와 해당 항목 이동·재시도 제공 |
| 입력 오류 | 오류 필드 가까이에 이유와 수정 방법 표시 |
| 서비스 설정 오류 | 생성 비활성화, 일반 메시지와 진단 코드만 표시 |
| 할당량/서비스 오류 | 내부 키 전환은 숨기고 최종 실패 시 재시도 시점 안내 |
| 저장 용량 초과 | 메모리 작업 유지, XLSX/텍스트 복사와 이력 정리 제공 |
| 클립보드 거부 | 선택 가능한 텍스트 영역으로 대체 |
| 확정 완료 | 짧은 상태 변화와 마지막 Point를 한 번만 사용 |

## 16. 반응형 동작

### 데스크톱

- 최대 콘텐츠 폭은 일반 앱 기준 1200px를 사용한다.
- 입력과 요약, 결과와 근거를 필요할 때 2열로 배치한다.
- 기능 1의 학생별 결과 표는 고정 헤더·고정 순번 열을 사용한다.

### 태블릿

- 기능 탭과 주요 행동을 유지하고 보조 패널을 한 열로 내린다.
- 결과 표는 가로 스크롤하며 주요 행동은 표 위에 고정한다.

### 모바일

- 학생별 표를 카드 목록으로 변환하고 한 학생의 핵심 입력을 먼저 보여준다.
- 헤더 기능 탭은 메뉴로 축약하지만 기능 이름을 숨기지 않는다.
- 생성과 내보내기는 가능하되 대량 검토에는 데스크톱 사용을 권장한다.
- 하단 고정 CTA가 콘텐츠·오류를 가리지 않도록 안전 여백을 둔다.

## 17. 접근성

- 모든 입력에 보이는 라벨과 오류 연결을 제공한다.
- 키보드만으로 파일 선택, 칩 선택, 표 편집, 재배정, 재생성, 확정과 메뉴 조작이 가능해야 한다.
- 포커스는 `design.md`의 Focus Blue와 2px 규칙을 사용한다.
- 상태 Point만으로 의미를 전달하지 않고 텍스트·아이콘을 함께 쓴다.
- 진행 변화는 과도하지 않은 `aria-live` 영역으로 알린다.
- 문장 유사도와 오류는 색뿐 아니라 점수·라벨·설명을 포함한다.
- 200% 확대와 모션 감소 환경에서 기능 손실이 없어야 한다.

## 18. 디자인 인수 체크리스트

- [ ] 기능 1·2·3, 공통 옵션, 데이터 관리와 접근 화면의 정상·빈·오류 상태가 설계되어 있다.
- [ ] 설정 탭, 학생 식별정보, 관찰 기록, 자동 붙여넣기와 API 키 UI가 없다.
- [ ] 기능별 생성 근거·검수·선택 재생성·확정·내보내기가 화면에 존재한다.
- [ ] 부분 실패와 결과 불명확 상태에서 성공 결과를 잃지 않는다.
- [ ] 승인된 favicon과 Creator credit 자산을 변경 없이 사용한다.
- [ ] 제품명이 D.Y. Kim 서명보다 우선하고 Brass 사용은 제한적이다.
- [ ] 키보드, 포커스, 상태 텍스트, 44px 조작 영역, 확대와 모션 감소를 충족한다.
- [ ] 모바일에서도 핵심 작업을 완료할 수 있고 복잡한 표는 카드 또는 안전한 가로 스크롤로 대체된다.
